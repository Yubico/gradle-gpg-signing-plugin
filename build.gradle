apply plugin: 'groovy'

repositories {
  mavenCentral()
}

dependencies {

  compile(
    gradleApi(),
    'commons-io:commons-io:2.5',
  )

}

version = '0.1.0'

task wrapper(type: Wrapper) {
  gradleVersion = '4.1'
}
